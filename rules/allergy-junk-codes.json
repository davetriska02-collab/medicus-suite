{
  "version": 1,
  "lastUpdated": "2026-07-29",
  "notes": "TWO categories of SNOMED allergy code live in this ONE list, distinguished by `category` -- both matched, flagged, and (when safe) ended by the exact same mechanism in content-scripts/allergy-junk-code-cleanup.js, so there's no reason to split them into separate files. `category:\"import-artefact\"` entries are NEVER a genuine allergy record under any circumstances -- pure import noise, safe to bulk-end outright. `category:\"too-generic\"` entries (added 2026-07-29) ARE occasionally used to carry a real allergy, just coded uselessly vaguely -- e.g. a real patient's peanut allergy found coded as free-text \"peanut\" in additionalInformation under 419076005 \"Allergic reaction\" rather than an actual substance code. These are NOT automatically safe to end: the widget's existing per-instance caution mechanism (buildCautionMessage, keyed on additionalInformation/severity/certainty/allergyReactions being populated -- see that function's own comment) is what makes including them here safe at all -- a too-generic code with NOTHING else attached is genuinely useless noise and gets offered for bulk-end same as an import-artefact entry, but the moment ANY clinical detail is attached, caution fires, the row is excluded from 'Select all', and the clinician has to look at it individually before deciding. FUTURE WORK, explicitly deferred by the user (2026-07-29), not built yet: when a too-generic code carries a real allergen in its free text (the peanut example), the useful move isn't to end it at all -- it's to CONVERT it to a proper substance-coded allergy using that free text as a search hint, an extension of the same idea as the caution mechanism, not yet designed. Matched by EXACT conceptId only (never a hierarchy/descendant search) for BOTH categories -- unlike rules/non-problem-root-codes.json's admin-code roots (broad categories with many real children needing a descendant search), each entry here is a single, self-contained SNOMED concept, not a root with meaningful children. Used by content-scripts/allergy-junk-code-cleanup.js's 'Bulk remove?' widget: fetches every ACTIVE allergy's own conceptId via GET clinical/data/allergy/overview-allergy/{allergyId} (the cheap clinical-summary/summary list has no conceptId, text only) and flags an exact match against this list. Each entry's own `reasonEnded` is submitted verbatim to POST clinical/allergy/end-allergy -- confirmed live 2026-07-29 that endpoint's payload is just {allergyId, endDate, reasonEnded}, with NO recordedByOrganisation field at all, so ending a junk allergy is never blocked by the blank-organisation validation lock that DOES affect converting one via change-allergy (see content-scripts/allergy-duplicate-merge.js's sibling feature, which handles that lock with the same 'Unknown' placeholder). Add a new entry only once a real patient's allergy list has confirmed it -- same discipline as every other code list in this codebase, never guess a plausible-sounding sibling in.",
  "codes": [
    {
      "conceptId": "716186003",
      "description": "No known allergies",
      "category": "import-artefact",
      "addedDate": "2026-07-29",
      "reasonEnded": "Redundant \"no known allergies\" marker -- the allergy list already shows this correctly on its own, without a coded entry",
      "rationale": "Confirmed live via overview-allergy (real patient), and independently reported by the user as a recurring pattern: a pre-defined allergy CODE recording the ABSENCE of allergies. This is defunct once entered -- Medicus's own allergy list already displays the correct \"no known allergies\" empty state automatically whenever a patient genuinely has no active allergy entries, so a coded entry saying the same thing adds nothing but visual noise, and per the user these frequently appear duplicated several times over a patient's import history (five identical copies confirmed on one real patient's clinical-summary list)."
    },
    {
      "conceptId": "161611007",
      "description": "H/O: non-drug allergy",
      "category": "import-artefact",
      "addedDate": "2026-07-29",
      "reasonEnded": "Import artefact -- a historic \"asked about allergies\" marker, not a genuine allergy record",
      "rationale": "Confirmed by the user (2026-07-29), not yet independently live-captured via overview-allergy for this specific concept (add a live confirmation note here once seen directly). Per the user: a historic marker code from a locally-commissioned service, used to record that a patient had been ASKED about allergies -- never intended to represent a genuine allergy in its own right -- but erroneously pulled into the actual allergy list by the import/GP2GP process (or possibly by Medicus itself). Per the user, these are often duplicated a dozen or so times across a patient's record history."
    },
    {
      "conceptId": "115665000",
      "description": "Atopy",
      "category": "too-generic",
      "addedDate": "2026-07-29",
      "reasonEnded": "Too generic to be clinically useful -- covers almost every allergic condition without identifying an actual allergen, and carried no other clinical detail",
      "rationale": "Confirmed by the user (2026-07-29): \"Atopy\" covers almost everything allergic -- coding an allergy this way conveys essentially no information about what the patient is actually allergic to. UNLIKE the import-artefact entries above, this code CAN legitimately carry a real allergy (e.g. via additionalInformation free text) -- see the top-level `notes` for the real peanut-allergy example that motivated adding this whole category, and buildCautionMessage's own comment for the mechanism that makes bulk-ending this specific code safe: it's only ever offered when NOTHING else is attached to the entry."
    },
    {
      "conceptId": "419076005",
      "description": "Allergic reaction",
      "category": "too-generic",
      "addedDate": "2026-07-29",
      "reasonEnded": "Too generic to be clinically useful -- does not identify an actual allergen, and carried no other clinical detail",
      "rationale": "Confirmed by the user (2026-07-29), and the DIRECT motivating example for the whole `too-generic` category: a real patient's peanut allergy was found coded under this concept with the allergen recorded only as free-text \"peanut\" in additionalInformation -- exactly the case buildCautionMessage's caution mechanism exists to protect (that entry would be flagged amber and excluded from 'Select all', never bulk-ended). Like Atopy, this code says only that SOME reaction to SOMETHING occurred, without identifying what -- useful only as a placeholder pending a proper substance code, never a genuinely complete allergy record on its own."
    }
  ]
}
