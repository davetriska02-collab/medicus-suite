{
  "version": 4,
  "lastUpdated": "2026-08-23",
  "notes": "ALL known GP2GP-import boilerplate found in a problem's additionalInformation free text lives HERE, in one place, distinguished by `kind` -- consolidated 2026-07-29 (previously split across this file and a second severity-defaulting-pattern.json). kind:\"literal\" entries (the original schema) are matched PER LINE, trimmed, case-insensitive exact match against `text` -- pure noise, always safe to strip on its own, no further action ever needed. kind:\"pattern\" entries are matched against the WHOLE additionalInformation text instead (never split by line first, so the same two-fragment boilerplate is recognised whether it lands on separate lines or run together on one) using `pattern`+`flags` as a JS RegExp source, compiled at read time -- used when the boilerplate ALSO encodes a captured VALUE worth checking against the problem's own structured data; an `action` object on the entry says what further step to offer for that captured value -- `severityCorrection` when the value can be confidently compared against the problem's own significance and auto-corrected on a genuine mismatch (severityDefaultingContradiction below), `reviewSeverity` when the captured value has no confirmed mapping to Major/Minor at all and the honest response is a prompt asking the clinician to check it themselves, never a guessed correction (sourceSystemPriorityValue below), `linkSuggestion` (2026-08-09) when the captured value is another problem's NAME rather than a severity -- content-scripts/problem-description-cleanup.js resolves it against the patient's other on-record problems via shared/problem-text-linking.js and offers to create an actual relationship (flat link, or nest either direction) when a confident match is found, or a plain review note when it isn't (groupedWithReference below). additionalInformation is often multi-line and can mix genuine free text with any number of these entries -- a genuine free-text fragment must never be silently dropped because the field ALSO happens to contain a generic one, whichever kind matched it. Used by content-scripts/problem-description-cleanup.js's stripGenericAdditionalInfoLines() (literal entries) and findPatternMatch()/computeAdditionalInfoFindings() (pattern entries, and the combined result). Add a new literal entry for a fixed boilerplate string with nothing further to check; add a pattern entry only when the boilerplate carries a captured value worth comparing against the record -- no code change needed for either.",
  "knownScopeLimitations": [
    "The \"Active Problem, ...\" family (Active Problem, Significant / Active Problem, Not Significant (Minor)) is DELIBERATELY matched as fixed kind:\"literal\" strings only, as of 2026-07-29 -- unlike sourceSystemPriorityValue's PRIORITY=n, the bracketed severity word in this template ((Major)/(Minor), and plausibly (Unspecified) for a problem with no significance set at all) is NOT extracted as a captured value or compared against anything structural. Practical consequence: a real 'Active Problem, Significant (Major)' or 'Active Problem, ... (Unspecified)' line would NOT currently be recognised at all -- only the two exact strings already confirmed live (no bracket, and '(Minor)') are matched; a bracketed '(Major)' sibling is a plausible but NOT yet observed variant, same 'confirm before adding' discipline as every other sibling in this file. This was a conscious choice, not an oversight: unlike severityDefaultingContradiction/sourceSystemPriorityValue, there is no known reason yet to believe a mismatch here (e.g. bracket says Major, structural significance says Minor) means anything different from what those other checks already catch -- generalising this family to a value-capturing pattern (mirroring severityDefaultingContradiction's approach) is a reasonable next step if/when a real example makes the case for it, not built pre-emptively."
  ],
  "entries": [
    {
      "kind": "literal",
      "text": "Active Problem, Significant",
      "addedDate": "2026-07-25",
      "rationale": "GP2GP import boilerplate restating metadata already captured structurally on the problem itself (problemStatus:\"Active\", significance:\"Major\"/\"Minor\") -- confirmed on a real patient's problem list, carries no information not already available elsewhere on the record."
    },
    {
      "kind": "literal",
      "text": "Active Problem, Not Significant (Minor)",
      "addedDate": "2026-07-29",
      "rationale": "Sibling boilerplate to \"Active Problem, Significant\" above, for the OPPOSITE significance value -- confirmed live 2026-07-29, real patient, seen alongside a second severity-defaulting fragment on the same record (\"Defaulted to Minor\\nProblem severity: Minor grade 1 with small erosion at GOJ\\nActive Problem, Not Significant (Minor)\") -- per the user, plausibly the result of the record having passed through more than one GP EPR/GP2GP transfer, each layering its own boilerplate onto the same field. Same restates-existing-metadata reasoning as its sibling (problemStatus:\"Active\", significance:\"Minor\"), no clinical meaning on its own. A \"(Major)\" counterpart is plausible by the same template but NOT yet seen on a real patient -- add it only once actually observed, same discipline as every other sibling pair in this file. Note the motivating example's SECOND line — \"Problem severity: Minor grade 1 with small erosion at GOJ\" — mixes the boilerplate \"Problem severity: Minor\" prefix with genuine clinical free text (\"grade 1 with small erosion at GOJ\") on the SAME line; severityDefaultingContradiction's pattern-match is span-based, not whole-line, so it correctly strips only the boilerplate prefix and leaves the genuine clinical text as its own line — no separate rule needed for that."
    },
    {
      "kind": "literal",
      "text": "Unspecified Significance: Defaulted to Minor",
      "addedDate": "2026-07-26",
      "rationale": "GP2GP import boilerplate: the source record carried no significance value, so the importer defaulted it to Minor and recorded that decision as a free-text line -- restates significance:\"minor\" already captured structurally on the problem, no clinical meaning on its own. Kept as a STANDALONE literal entry in addition to being half of the severityDefaultingContradiction pattern below, so a copy of this line with no paired \"Problem severity:\" line at all is still recognised and stripped as plain noise, not left untouched."
    },
    {
      "kind": "literal",
      "text": "Defaulted to Minor",
      "addedDate": "2026-07-29",
      "rationale": "A SHORTER-PREFIX sibling of \"Unspecified Significance: Defaulted to Minor\" above, confirmed live 2026-07-29 (real patient, reported not flagging): the same line without the leading \"Unspecified Significance: \" -- some records carry only \"Defaulted to Minor\" on its own. Same restates-existing-metadata reasoning; kept as a STANDALONE literal entry for the same reason as its longer sibling (a solo/unpaired occurrence is still recognised), and the paired case is additionally covered by the now-generalised severityDefaultingContradiction pattern below, whose prefix is optional for exactly this reason."
    },
    {
      "kind": "literal",
      "text": "Problem severity: Minor",
      "addedDate": "2026-07-26",
      "rationale": "Sibling boilerplate line to \"Unspecified Significance: Defaulted to Minor\" above, same real example, same restates-existing-metadata reasoning. Kept standalone for the same reason -- a solo/unpaired occurrence is still recognised."
    },
    {
      "kind": "literal",
      "text": "Problem severity: Major",
      "addedDate": "2026-07-29",
      "rationale": "Sibling to \"Problem severity: Minor\" above, now confirmed on a real patient (HAR capture, SCTID 433144002 'Chronic kidney disease stage 3'). Kept standalone for the same reason as its siblings; the paired case (alongside an \"Unspecified Significance: Defaulted to ...\" line) is additionally covered by the severityDefaultingContradiction pattern below, which is what actually drives the severity-correction action."
    },
    {
      "kind": "literal",
      "text": "SUMMARY=Y",
      "addedDate": "2026-08-20",
      "rationale": "Nick's own real-record example, 2026-08-20 — another raw source-system field flattened into free text, same family as sourceSystemPriorityValue's \"PRIORITY=n\" below but with no captured value worth checking against the record: unlike PRIORITY, there's no confirmed Major/Minor (or any other) mapping for this flag, and no evidence yet that it ever varies (a \"SUMMARY=N\" sibling is plausible by the same template but NOT yet observed — add it only once actually seen, same discipline as every other sibling in this file). Kept as a plain kind:\"literal\" entry rather than a value-capturing pattern for that reason: nothing here to act on beyond removing the noise itself."
    },
    {
      "kind": "pattern",
      "id": "severityDefaultingContradiction",
      "pattern": "(?:Unspecified Significance: )?Defaulted to (Major|Minor)\\s+Problem severity: (Major|Minor)",
      "flags": "i",
      "addedDate": "2026-07-29",
      "rationale": "Confirmed real-world layouts of the same GP2GP template, all matched by this one pattern: joined by a newline (HAR capture, 2026-07-29, SCTID 433144002 'Chronic kidney disease stage 3' -- \"Unspecified Significance: Defaulted to Minor\\nProblem severity: Major\", stored significance:\"minor\" while the source record said Major), joined by a single space on ONE line with no newline at all (also confirmed live, 2026-07-29, values agreeing this time -- \"Unspecified Significance: Defaulted to Minor Problem severity: Minor\"), and WITHOUT the leading \"Unspecified Significance: \" prefix at all (confirmed live 2026-07-29, real patient, reported not flagging -- \"Defaulted to Minor\\nProblem severity: Minor\") -- the leading `(?:Unspecified Significance: )?` group is optional for exactly this reason, and `\\s+` between the fragments tolerates the newline-vs-space variation independently of the prefix. The regex engine still captures the FULL matched span (prefix included, when present) so `cleaned` never leaves a dangling \"Unspecified Significance: \" behind. Group 1 is what the importer defaulted TO; group 2 is what the source record's own free text actually said the severity was. When group 2 disagrees with the problem's own stored significance, the record has almost certainly been mis-filed by the import -- see the `action` below and content-scripts/problem-description-cleanup.js's computeAdditionalInfoFindings. When group 2 agrees with the stored significance, this reduces to plain noise and is stripped with no further action, same as any literal entry.",
      "action": {
        "type": "severityCorrection",
        "capturesDefaultedTo": 1,
        "capturesStatedSeverity": 2
      }
    },
    {
      "kind": "pattern",
      "id": "sourceSystemPriorityValue",
      "pattern": "PRIORITY\\s*=\\s*([0-9]+)",
      "flags": "i",
      "addedDate": "2026-07-29",
      "rationale": "Some source GP systems export a problem's severity/priority as a raw \"PRIORITY=n\" field (n observed 1-9, but the pattern isn't restricted to a single digit or that range since the exact bounds aren't confirmed -- widen the observed range in this note, not the regex, once a real out-of-range example is seen). UNLIKE severityDefaultingContradiction above, there is NO confirmed, consistent mapping from most of this scale to Medicus's Major/Minor significance -- different source systems plausibly use different scales for most values, and none has been reverse-engineered. The ONE exception is n=1 (see `valueSeverityMap` below): confirmed by the practice's own direct experience running the source system this convention came from, and holding across every example seen in imported data since -- n=1 reliably means Major on that system specifically. Every OTHER value still gets the plain informational prompt, never a guessed correction; only n=1 is confident enough to auto-suggest. Still flagged as junk/noise once reviewed either way -- the raw \"PRIORITY=n\" text carries no meaning Medicus can act on structurally beyond that one mapped case, so it's offered for removal the same as any other entry here.",
      "action": {
        "type": "reviewSeverity",
        "capturesPriorityValue": 1,
        "valueSeverityMap": { "1": "major" }
      }
    },
    {
      "kind": "pattern",
      "id": "episodicitySuffix",
      "pattern": "\\{Episodicity\\s*:[^}]*\\}|Episodicity\\s*:\\s*code=[0-9]+\\s*,\\s*displayName=[^\\s,.;}]+",
      "flags": "i",
      "addedDate": "2026-08-14",
      "rationale": "GP2GP reimport wrapper -- confirmed live 2026-08-14 on a real journal note whose ENTIRE `note` free text was nothing but this block: \"{Episodicity : code=255217005, displayName=First}\". Already a known, cross-confirmed signature in this codebase before today, just never previously added to THIS rules file: engine/record-duplicate-parser.js's EPISODICITY_SUFFIX_RE (used there for duplicate-detection text normalisation, a different purpose from this file's strip-and-offer-removal one) documents the same pattern confirmed live 2026-07-08 (a 'Perianal abscess' note pair) and again 2026-07-17 (docs/learnings-vaccination-note-duplicates.md, an umbrella-problem reused across a patient's entire history). Pure reimport noise -- the {code, displayName} pair restates a source-system episodicity classification not surfaced anywhere else on the Medicus record, and carries no clinical meaning of its own once flagged; no `action` needed, same as any plain literal entry. Content-script (not JSON) reason this couldn't just reuse record-duplicate-parser.js's own regex constant directly: that file is loaded only in the duplicate-checker.html bundle, not the medicus.health content-script group (same 'no cross-bundle imports' constraint shared/journal-problem-matching.js's own header documents) -- the pattern is duplicated here deliberately, not by oversight, and the two copies should be kept in sync if this regex is ever revised. UPDATED 2026-08-23: confirmed live on a real ALLERGY's additionalInformation with the SAME code=255217005/displayName=First values but NO surrounding braces at all -- \"Episodicity : code=255217005, displayName=First\" -- so the pattern is now an alternation: the original braced form (which still swallows anything up to the closing brace, no matter what it contains, same as before) OR a bounded unbraced form matched specifically as `code=<digits>, displayName=<value>` (value stops at whitespace/comma/period/semicolon/brace, so it can't run on and swallow genuine free text that happens to follow on the same line). The unbraced form is deliberately narrower than the braced one BECAUSE it has no closing delimiter to bound an open-ended '[^}]*' against -- widen it only once a real example proves the confirmed shape wrong, same discipline as every other entry here."
    },
    {
      "kind": "pattern",
      "id": "gp2gpProblemNotesPrefix",
      "pattern": "Problem Info:\\s*Problem Notes:\\s*",
      "flags": "i",
      "addedDate": "2026-08-14",
      "rationale": "The other half of the same GP2GP reimport wrapper signature as episodicitySuffix above -- independently confirmed (engine/record-duplicate-parser.js's GP2GP_WRAPPER_RE, 'seen on an independent \"Pigmented naevus\" problem AND a note, so treated as a confirmed signature, not a one-off') but not yet directly observed on a journal note's own text within a live capture for THIS file specifically -- added now on the strength of that prior cross-confirmation, same as episodicitySuffix's sibling entry, since the two fragments are confirmed to appear independently of each other (episodicitySuffix's own real example had NO prefix at all -- the whole note body was only the suffix block), not as a single combined pattern. A separate entry, not a combined prefix+suffix regex, for that reason -- either can be present without the other."
    },
    {
      "kind": "pattern",
      "id": "groupedWithReference",
      "pattern": "\\(Grouped with ([^)]+)\\)",
      "flags": "i",
      "addedDate": "2026-08-09",
      "rationale": "Per Nick's direct identification on real patient records, 2026-08-09 (verbatim example: \"(Grouped with Anxiety with depression)\"): GP2GP import boilerplate that flattens a structured problem-grouping/relationship from the source system into free text, naming ANOTHER problem on the same patient's record by description rather than by ID. Unlike severityDefaultingContradiction/sourceSystemPriorityValue, the captured value here is not a severity at all -- it's a problem NAME to resolve against the patient's own other problems (see content-scripts/problem-description-cleanup.js's use of shared/problem-text-linking.js's matchProblemByName). A confident match means this text is almost certainly describing a real linked-problem or parent/child relationship that was never actually created in Medicus -- offered as an action, not auto-applied, same as every other correction in this file. No confident match still gets a plain review note (never a guessed link) and the boilerplate itself is still offered for removal either way.",
      "action": {
        "type": "linkSuggestion",
        "capturesProblemName": 1
      }
    }
  ]
}
