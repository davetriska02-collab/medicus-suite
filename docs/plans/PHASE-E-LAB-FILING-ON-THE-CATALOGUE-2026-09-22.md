# Phase E — Lab Filing on the Lab Result Catalogue

**Status:** DESIGN AGREED IN PRINCIPLE (Nick + virtual Dave, 2026-09-22). BUILT so far: whitelist matching fix (v3.265.1), test-card layout (v3.265.2), per-result x lab x code ranges + enable + own approval, data and screen (v3.266.0). NOT built: safety guards / filing controls columns, the "never offer to file" strip, per lab-group comment whitelist, the adapter / union-only gate / shadow log, migration. Depends on v3.265.0 (Phase D) being merged.
**Related:** `docs/plans/LAB-RESULT-CATALOGUE-DATA-MODEL-2026-09-19.md` (§5.1), `docs/plans/PHASE-D-OUTSTANDING-MATCHER-ON-CATALOGUE-2026-09-22.md`, hazards H-073, H-074, H-075 (existing); H-080 / H-081 proposed (below).

---

## 1. The conceptual framework (Nick, 2026-09-22)

This is the model every later decision hangs from.

**Building block: individual results / analytes** (haemoglobin, ALT, HbA1c).
- Per result, accommodate different lab handling — e.g. differing normal ranges between labs.
- Also different acceptable ways of reporting — e.g. HbA1c NGSP (%) vs IFCC (mmol/mol).
- Within the GP system, different SNOMED codes may be used for the same result.

**Request groupings.** In the GP system we order "FBC" for convenience. This is *not a thing* — it is shorthand for a group of tests we want run.

**Lab report groupings.** The lab takes that shorthand and runs some or all of the tests, then sends them back as groups of results. Each group is, too, *not a thing* — a convenient way of bundling results. Groups matter only to the extent that:
- they correspond to request groupings, for marking requests as completed; and
- they can carry **comments**, which may relate to the whole group or to an individual result within it.

### What follows from it
| Belongs to… | Because |
|---|---|
| the **result** (per lab, per reporting form) | normal ranges, unit, trend limit — the lab does not process ALP differently for a "Bone profile" than for an "LFT" |
| the **lab + report group** | whitelisted lab comments — a comment arrives as one lab-generated package for a lab-defined group, and only *sometimes* names a result |
| request / report groupings | recognition and "mark request completed" (Phase D, done) — nothing else |

So **a practice normal range is stored once per (result × lab × SNOMED code)** — the unit is carried by the code (HbA1c IFCC and NGSP are different codes), so it is not entered separately —, never per test or per group. Editing ALP's range in "LFTs" and seeing it change in "Bone profile" is *correct* — it is one result. (The catalogue already stores results once; the Investigations page says "Used by N tests".)

## 2. What is stored today (so the gap is clear)

- Legacy filing profiles (`labfiling.profiles`) each carry their own free-text `parameters[]` (analyte name + low/high/unit) matched **by name text**. ALP in an LFT profile and ALP in a Bone profile are two unrelated entries; nothing is per lab.
- The lab's own reference range and high/low flag arrive with every report and are used at filing time (`r.low`, `r.high`, `isAbove`, `isBelow`). Nothing is stored from them. A practice range is an optional override (`paramsOverrideLabFlags`) or a substitute for an un-ranged result.
- Catalogue results have no range field yet. Needed: `ranges: [{ lab, unit (or code), low, high }]` on the result.

## 3. Design

### 3.1 Data (all in the catalogue overlay; inert until approved)
- **Result × lab filing setup** (one row of the table): practice range low/high (+ unit/reporting form), trend limit, safety-guard set, filing controls (visible text of the "Normal result, no action required" option / File button — defaults as today), `assisted filing enabled` flag, own **filing approval** (separate from the test-matching approval; approving one never approves the other).
- **Lab × report-group comment whitelist**: `[{ lab, groupHeading, phrase }]` — whole lab-generated comment text.
- **Decided (Nick, 2026-09-22):** `excludeIfMeds` is **per result**. "Block if the comment says…" (`suppressIfText`) and the comment whitelist are **per lab group** — only because that is how comments are sent; they usually name one result but parsing that is too unreliable, so the whole report is blocked unless the comment is whitelisted.
- **A report is offered for one-click filing only if:** every result in it is recognised by code (H-074 generalised), has an enabled + approved filing setup for that lab, passes its own range/trend/guards, and every comment residue is fully covered by that group's whitelist. One combined-bloods task passes only if *every* result passes — so "strictest-wins merging" stops being special code: each result carries its own guard and the whole task is blocked if any one blocks.

### 3.2 Whitelisted comments — matching rule (a safety change, see §5) — **matching rule, size floor and no-truncation IMPLEMENTED 2026-09-22 in `shared/lab-filing-utils.js` (tests in `test-lab-filing-utils.js`); per lab × group keying and practice-level approval still to do**
Real samples so far (2026-09-22): the lab's LFT note (≈304 characters, 45 words, 5 sentences); the eGFR/NICE note in its G2 variant (≈597 characters, 97 words, 6 sentences); and Nick's two stored entries — the G1 eGFR note and one entry that is two AKI-risk comments joined into one phrase (a single-comment residue is no longer excused by that joined entry: whitelist each comment separately).
- **Matching:** the whole comment residue must be explained by whitelisted text (one phrase, or several tiling it). No "residue *contains* phrase" and no "phrase contains residue". A whitelisted phrase + appended warning must **not** be excused.
- **Storage limit:** `allowComments` items used to be silently truncated to 500 characters. Nick's own stored entries are all under that (the G1 eGFR note is ~350–460 characters), but the longer G2 variant of the same lab note ("Mildly reduced eGFR, this may be appropriate…") is ~600 characters and could not have been stored whole; containment matching would have let a truncated copy still match. Limit raised to 2 000; **reject, never truncate**.
- **Minimum size (guard-rail, secondary to the rule above):** at least 6 words and 30 characters; reject entries made only of generic words (`normal`, `no action`, `within normal limits`). The generic-word list is to be reviewed if imaging (X-ray) reports ever trigger it regularly (unlikely assisted filing candidates).
- **Converting existing rules:** when the existing assisted filing rules are converted, their whitelisted comments are converted as they are and are EXPECTED TO FAIL (truncated / partial entries no longer excuse the whole comment) — that proves the tightened logic; they are then re-whitelisted from the real residue.
- **Who decides:** whitelisting is a practice-level decision. A local entry that is not in the practice-approved set does not act; a practice decision overrides local ones. Turning on is per **clinician**, not per machine.

### 3.3 Sync / approval (deferred — recorded so it is not lost)
Approvals maintained and enforced at practice level and polled out (same mechanics as code-cleanup); a central review/approval dashboard before anything polls out. Per-PC re-enabling of dozens of profiles is explicitly rejected as unworkable; current force-disabled-on-arrival stays until the practice-level mechanism exists.

### 3.4 Screen (Options → Investigations, each test card)
Row 1 (two columns): *How it is requested in Medicus* | *How it comes back from the lab*.
Then full-width rows: the two different "never" strips — **"Never counts as this test"** (matching, unchanged) and **"Never offer to file when…"** (filing, in the assisted filing colour) — then **one SNOMED table** (Name | Code | Core/Optional | Wordings | Units | Lab) with a differently coloured assisted filing section (practice range min | max | Safety guards | Filing controls | Enable assisted filing). Guards/controls open the existing Lab Filing panes; defaults "File results" / "Normal result, no action required". List filter and card tag: assisted filing enabled / disabled. A change to a range withdraws that result's filing approval.

### 3.5 Revisions agreed 2026-09-22 (later) — supersede anything above that says otherwise
- **Assisted filing is switched on and approved for a TEST at a LAB, never per result.** Medicus files a whole report group at once. The on/off switch lives on the lab x report-group entry (`filing.groups[].enabled`); ONE approval (`approveFilingForTest`) covers the report group(s) that identify the test, the practice ranges and safety guards of the test's results at that lab, the group's lab-comment settings, and the Medicus wording if changed. Any later change reopens only the item changed. The list badge ("assisted filing — awaiting approval") opens the test at its assisted filing bar; approval is only ever given there, where what it covers is shown.
- **Trend guard has a direction:** never offer to file if it has changed / increased / decreased by more than X% (an eGFR that rises or a creatinine that falls is good news; the opposite is not).
- **Medicus filing-screen wording is not lab-specific:** one practice-wide setting, pre-filled with the standard wording ("Normal result, no action required", "File results"), stored only if changed, needing approval only if changed. It never changes what is written to the record: the macro finds these controls on the live screen by their visible text, so it only has to match what Medicus shows (a Medicus wording change would otherwise make the macro fail closed).

## 4. Rollout (agreed)
- **E0** pure adapter + golden/differential tests + migration proposals with a **parity test** (every legacy `analytes` entry maps to a catalogue result, or the migration refuses and the legacy profile keeps running). Unwired. **NOT DONE** — migration proposals from `labfiling.profiles` were never built; E2 shipped without them (nothing required them — catalogue filing setup is authored directly on the Investigations page instead).
- **E1** the catalogue may only ADD blockers to the live gate (union-only); a **separate, mandatory shadow log** (names, codes, blocker reasons, counts — never values) records where the catalogue would have unblocked something legacy blocks. **DONE** (`engine/lab-filing-catalogue.js`, `engine/lab-filing-gate.js`), committed 2160e494 (v3.269.0).
- **E2** opt-in `filingEngine` pref (default legacy, practice-profile published), per-result-and-lab enable, filing approval. `defaults.json` version bump + regen + lock. **DONE 2026-09-24**: `filingEngineWanted()`/`combineWithCatalogueIfWanted()` in `content-scripts/triage-lens/lab-file-button.js`, run from BOTH `evaluateGate()` (poll-time, what's offered) and `onAction()` (click-time re-verification, what actually proceeds) so the two can never disagree; pref lives in `triagelens.config.prefs.filingEngine` (NOT `labfiling.config`) for `defaults.json` governance (version bumped 26→27) and practice-wide publishing (`shared/io/practice-profile.js`, mirroring `oirEngine`'s exact mechanics); UI on the Lab filing settings page (`options/labfiling-section.js`, cross-module write into `triagelens.config` — the right storage home, the right UI home, deliberately not the same file). Per-result-and-lab enable + filing approval were already built in the earlier Investigations-page assisted-filing work. **UNCOMMITTED, not yet live-tested** — see the session note below.
- **E3** replacing legacy gates only after a stated period with no "would have unblocked" cases — CSO decision. Not started.
- Audit records **engine** (legacy | catalogue | catalogue-fallback) at file time — done. Catalogue **version** and explicit **approval state** fields were NOT added (no natural "catalogue version" value exists yet; approval state is implicit today, since an unapproved group/result always produces a blocker) — flagged as a gap, not silently claimed done.

### Session note (2026-09-24)
E2 built and unit-tested (`test-lab-filing-gate.js`, `test-practice-profile.js`) but genuinely UNCOMMITTED and NOT YET LIVE-TESTED against a real Medicus filing screen with `filingEngine: 'catalogue'` actually selected. Before treating E2 as safe to roll out to any real practice machine: (1) live-test with the pref OFF (confirm zero behaviour change — the default), (2) live-test with it ON against a result that should file cleanly, (3) live-test against a result the catalogue should additionally block that legacy alone would have let through (proves the union-only "more cautious, never less" contract actually holds live, not just in unit tests), (4) CSO to move H-080/H-081 from "proposed" into the real `docs/HAZARD-LOG.md` now that the control they describe is code-complete, not just planned.

### Session note (2026-09-25) — the catalogue engine can now operate with ZERO legacy profiles
Nick's live test: deleted all legacy Lab Filing profiles to test "catalogue only", `filingEngine: 'catalogue'` set — nothing triggered. Root cause: `evaluateGate()`'s very first content check was `if (!profiles.some(p => p.enabled)) { hideButton(); return; }` — it hid the button before the catalogue engine was EVER consulted, whenever no legacy profile was enabled, regardless of `filingEngine`. This was never noticed before because E2 was designed and tested (unit-level only) around "catalogue ADDS to an already-matched legacy profile" — a genuinely profile-free scenario was never exercised.
**Fixed**: that early exit, and the "no profile matched" exit right after it, now only fire when the catalogue engine is ALSO not opted in. With no legacy profile but the catalogue engine on, the catalogue's own practice-wide screen wording (`catalogue.filing.screen`, or the standard defaults if unset) locates the File control instead of a profile's. Blocker computation is kept carefully separated from profile MATCHING: a `null` legacy profile flows through cleanly (the blocker functions in `shared/lab-filing-utils.js` already no-op gracefully on a null profile) — but a synthetic profile-shaped OBJECT must never be built for that purpose, because a profile object with an empty `.analytes` list makes `unrecognisedAnalyteBlockers` flag EVERY result as unrecognised (the opposite of the intended behaviour). A separate synthetic `displayProfile` (name/commitMode/screen text only, nothing legacy-specific) is used ONLY for the button UI and the DOM-interaction macro, which do need a profile-shaped object. `evaluateGate()` and `onAction()` now track `currentLegacyProfile` (real match, possibly null) separately from `currentProfile` (always a real object when offering, may be the synthetic one) so the two never disagree.
**Known limitation, not yet fixed**: the profile-agnostic baseline check (`fileabilityBlockers`/`unresolvedCommentedResults`, which the catalogue-only path now relies on for its "floor") flags ANY non-benign lab comment as unresolved when there is no legacy profile to supply a whitelist — it does not yet know to fall through to the catalogue's own per-lab-group `allowComments` list for this. So today, catalogue-only mode is only useful for UNcommented reports; a commented report will block even if the catalogue's own whitelist would have permitted it. If Nick's retest still doesn't trigger, this is the next thing to check.

### Session note (2026-09-25, continued) — TWO more live-caught issues, both fixed
Nick's retest with the screen-wording match: "Now popping up!" — but flagged a genuine safety bug: **"the assisted filing toggle is firing even when a catalogue rule is awaiting approval. The logic there needs to check first that the matching rule / reference ranges are approved, THEN whether assisted filing is enabled."**

**Root cause (real bug, H-081 territory)**: a report-GROUP's own approval (`filing.groups[].enabled` + reviewed) correctly blocks the WHOLE heading when unapproved — that part was already fail-closed. But a single RESULT's practice RANGE or safety GUARD, edited AFTER its group was already approved (which independently resets THAT range/guard's own `provenance.reviewed` to false — "any change withdraws approval"), was invisible to `engine/lab-filing-catalogue.js`: the acting (approved-only) catalogue simply has NO ENTRY for it any more (excluded by `attachFiling`'s `includeUnreviewed:false` filter), which `resultBlockers()` could not distinguish from "no range was ever configured" — so it silently fell through to the LAB's OWN reference range/flag. If the lab's own range happened to say the value was normal, **zero blockers were produced for a result whose practice range had a pending, unapproved edit sitting on it.**

**Fixed**: `engine/lab-filing-catalogue.js`'s `evaluateFilingCatalogue` gained an optional `opts.pendingCatalogue` — the SAME merge as the acting catalogue but with `includeUnreviewed:true` (every filing entry then carries its own `reviewed` flag instead of unreviewed ones being silently absent). New `findPendingRange`/`findPendingGuard` check it ONLY when the approved-only lookup came back empty, distinguishing "never configured" (unaffected — still falls back to the lab's own range, correctly) from "configured, awaiting approval" (now BLOCKS, with reason kinds `pending-range`/`pending-guard`). `content-scripts/triage-lens/lab-file-button.js` fetches this second catalogue view (`ensureFilingCataloguePending()`, cached + invalidated the same way as the approved-only one) and threads it into BOTH the real E2 combine and the E1 shadow log. Recognition (investigations/results/codes) deliberately still comes ONLY from the approved-only catalogue — this fix only closes the range/guard gap, it does not loosen anything.
Tests: `test-lab-filing-catalogue.js` (7 new checks: the exact before/after repro, pending-guard, and confirmation the golden/never-configured paths are unaffected), `test-lab-filing-gate.js` (4 new wiring checks). Full suite 435/436 (same pre-existing unrelated failure), lint clean.

**Separately fixed** (real crash, not a safety-logic gap): `computeProfileBlockers` read `profile.excludeIfMeds` with no null-guard — once the catalogue-only path legitimately made `profile` `null`, this threw as an UNCAUGHT PROMISE REJECTION on every single poll, silently aborting `evaluateGate()`/`onAction()` partway through (nothing after it — shadow log, E2 combine, the button OR the blocked card — ever ran). Audited every other `profile.` access in the file for the same pattern; this was the only unguarded one.

**Also reverted this session**: the investigations-list "needs-attention" weighted sort (`options/investigations-section.js`, added 2026-09-24) — Nick: "in practice... they look random." Back to plain alphabetical; the four Any/Yes/No toggles + presets remain as the way to find what needs work.

### Session note (2026-09-25, continued further) — pending-approval fix confirmed working live ("Working, thank you"); two more asks
Nick, after confirming the pending-range fix works: (1) the range/guard fix is good; (2) a NOTE (explicitly deferred — "that's fine for now") that filing is currently two clicks (Review & file all normal → a second popup elsewhere to read → File) when it should eventually be one; (3) BUILT — "the checkbox to grab whitelist lab comments from the popup", i.e. the catalogue-engine equivalent of the legacy "whitelist this comment" checkbox already in the blocked card.

**Built**: `engine/lab-filing-catalogue.js`'s `evaluateFilingCatalogue` now ALSO returns structured `unresolvedComments: {name, residue, labId, heading}[]` (alongside the existing human `blockers` and value-free `reasonKinds` — the shadow log still reads only `reasonKinds`, never this). `content-scripts/triage-lens/lab-file-button.js` carries this through `combineWithCatalogueIfWanted` into a SEPARATE checkbox box (`renderCatalogueWhitelistBox`/`whitelistSelectedCatalogueComments`, distinct from the legacy one — different target, different persistence) that writes a ticked comment into the (lab, heading) report group's `allowComments` via `OV.setFilingGroup`, explicitly setting `enabled: false` (same "any change re-opens review, and switches off" safety posture as the legacy save) and persisting via `labcatalogueSaveOverlay`. Since a `labcatalogue.practice` write doesn't trigger the same re-render path a legacy `STORE_PROFILES` write does, the save explicitly calls `scheduleEval()` afterward.
**One-click filing** (the deferred note above): not started. The likely path once picked up — the catalogue-only `displayProfile`'s `commitMode` is currently hardcoded to `'manual'` (pre-fill only, human clicks File); switching it to `'confirm'` (or exposing a commitMode choice, practice-wide, alongside the Medicus filing-screen wording setting) is the natural fix.
Tests: `test-lab-filing-catalogue.js` (+3: structured shape, residue/lab/heading correctness, no-entry on an already-allowed comment), `test-lab-filing-gate.js` (+7 wiring checks, +1 fixed for the return-shape addition). Full suite still 435/436 (same pre-existing unrelated failure), lint clean. UNCOMMITTED, not yet live-tested.

### Session note (2026-09-25, live-tested) — the write worked, but the whitelist still didn't take effect: a second, genuinely separate bug
Nick's exact repro: whitelisted the comment on the blocked card (confirmed correct — the group went `enabled:false`, matching profile/test stayed recognised); went to Options → Investigations, saw the comment WAS there under "Lab comments allowed through" (screenshot confirmed the write landed); re-approved the group. Still blocked, same generic wording ("carries a comment the suite cannot score... — read the comment before filing"), and now with NO checkbox offered (nothing left for the catalogue engine itself to flag).

**Root cause**: that wording is NOT the catalogue engine's own `comment-not-whitelisted` reason — it's the SUITE'S OWN baseline check (`effectiveScore()` → `LF.fileabilityBlockers()` → `unresolvedCommentedResults(report, profile, matchedProfiles)`), which runs UNCONDITIONALLY regardless of engine. With no legacy profile (catalogue-only mode), `profile` is `null` there — meaning the baseline has NOTHING to check a comment against and treats EVERY non-benign comment as unresolved, forever, no matter what the catalogue says. This is exactly the "known limitation" flagged in the FIRST 2026-09-25 note above — it stopped being a deferred nice-to-have the moment it silently ate a real live test.
**Fixed**: `effectiveScore()` now takes an optional `catalogueAllowComments` param — when there's no legacy profile, it builds a comment-ONLY pseudo-profile (`{ allowComments }`, nothing else) from every report group's allowComments practice-wide (not narrowed to this report's own lab — simpler, and safe: the catalogue engine's own PRECISE per-lab-per-heading check still independently re-blocks a comment only allowed for a different heading, so this can only make the baseline as permissive as the catalogue's own correct check already is, never more). Critically, this pseudo-profile is used ONLY inside `effectiveScore()`'s two `fileabilityBlockers` calls — never passed to `computeProfileBlockers` (which must keep seeing a genuinely null profile, or `unrecognisedAnalyteBlockers` blocks every result — the bug from the FIRST fix earlier this session). `evaluateGate()` and `onAction()` both now compute this and pass it through, reusing the already-fetched/cached catalogue (no new fetch cost).
Tests: 5 new wiring checks in `test-lab-filing-gate.js` (73 total in that file now). Full suite still 435/436, lint clean. UNCOMMITTED — this needs the SAME live retest (whitelist → re-approve → confirm it actually files now) before trusting it.

### Session note (2026-09-25, continued) — H-081 control d relocated to the report group; one-click filing done
Two follow-ups from Nick's live retest of the eGFR override fix above.

**overrideLabFlag moved off the per-result guard onto the report group.** Nick, having built the whole system, still couldn't find the per-result checkbox buried in each result's own "Safety guards" editor ("if I didn't find it having written this system, no chance of a mere user doing so"), and a multi-result test needed it ticked once per result. `shared/lab-catalogue-overlay.js`: `sanitiseFilingGuard` loses the field, `sanitiseFilingGroup` gains it; `setFilingOverrideForTest` (new, mirrors `setFilingForTest`'s "every heading variant kept in sync" pattern) sets it across every report-group entry for a test at a lab in one call; `filingStateForTest` now also reports `overrideLabFlag`. `setFilingForTest` and the comment-whitelist writer (`lab-file-button.js`) were both fixed to preserve an existing override when they rewrite the SAME group entry for an unrelated reason (enabling filing, whitelisting a comment) — `upsertFiling`'s "any change withdraws approval" is a full-entry replace, so omitting the field would have silently reset it to off. `engine/lab-filing-catalogue.js`'s `resultBlockers`/`applyCatalogueOverrides` now read it from the result's own report-group entry instead of a per-result guard. UI: the checkbox moved from `renderGuardsEditor` (per result) to `autofilingBar` (once, in the "Assisted filing on…" bar), wired to `setFilingOverrideForTest`. Tests: new dedicated block in `test-lab-catalogue-filing.js` (independence from the on/off switch, approval re-opening, and the "toggling one switch must not wipe the other" regression), plus the affected fixtures in `test-lab-filing-catalogue.js`/`test-lab-filing-gate.js`/`test-labcatalogue-io.js` updated.

**One-click filing (the deferred note two sessions back — now done).** The catalogue-only `displayProfile` (`evaluateGate()`, `lab-file-button.js`) had `commitMode` hardcoded to `'manual'` (pre-fill only — the clinician then had to ALSO press Medicus's own File button, the double click Nick flagged and deferred as "fine for now"). Switched to `'confirm'`, the natural fix already anticipated above: one click on the suite's button, one `window.confirm()` dialog listing every value (the SAME dialog a legacy profile set to `confirm` already uses — nothing new, no weakened gate; H-081's own gate list already names both modes), then the macro presses File itself. The button label falls out of the existing `mode === 'manual' ? 'Review & file all normal' : 'File all normal…'` ternary with no further change. Test: `test-lab-filing-gate.js` regex updated for the new literal + one new wiring check.
Full suite 435/436 (same pre-existing unrelated failure), lint clean. UNCOMMITTED, not yet live-tested — needs a live retest that catalogue-only filing now completes in one click with the confirm dialog.

## 5. Gates that must never be weakened
Numeric-only "every numeric result needs a lab or practice range" (text results need none); unreadable-row block; parameter-ambiguity block (goes away with code-keyed ranges once dual-running stops); unit agreement; comparator-censored (">47") fail-closed and never clears a lab flag; H-074 "every result must be recognised"; `commitMode` manual|confirm only (in *manual* the person presses File; in *confirm* the person confirms a dialog listing every value and the macro then presses File); inert until reviewed and enabled.
**Existing weakness to fix first:** `_commentAllowedByProfile` matches by containment in both directions with no minimum length (H-073 open review item) — see §3.2.

## 6. Proposed hazards (not yet in `docs/HAZARD-LOG.md`)
- **H-080** — Catalogue-driven filing offers a wrong one-click "all normal". Controls: union-only, separate mandatory shadow, per-result-and-lab enable, separate filing approval, fail-closed code-based recognition, human confirm.
- **H-081 (revised)** — A practice range or guard attached to a result acts where it does not belong: a different lab, a different reporting form or unit (HbA1c %, mmol/mol; eGFR equation), or a result mis-identified as that analyte. Controls: key = (result × lab × unit/reporting form); unit must positively match; a range edit withdraws approval; code-based identification; range shown beside the lab's own range.
- **Amend H-073** (whitelist now per lab × group, matching rule, practice-level) and **H-074** (recognition by code / catalogue membership).
