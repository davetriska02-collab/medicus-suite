# macOS
.DS_Store

# Editor files
.vscode/
.idea/
*.swp
*~

# Build artefacts (the GitHub Actions workflow creates these)
*.zip
!medicus-suite-baseline.zip
*.crx
dist/

# CRX signing key — the extension ID derives from it. NEVER commit; the CI copy
# lives in the CRX_PRIVATE_KEY Actions secret. See docs/IT-DEPLOYMENT.md.
keys/

# Local dev scratch
node_modules/
*.log

# Patient / sensitive data — NEVER commit
data/sars/
uploads/
output/

# Cowork skill staging — for the medicus-suite-deploy skill, copied to
# /mnt/skills/user/ during repo setup. Not part of the repo itself.
_skill/

# Claude Code agent worktrees (scratch, never commit)
.claude/worktrees/

# design-sync (Claude Design) — transient build artifacts & deps; durable
# inputs (design-system/ src, .design-sync/{config.json,conventions.md,previews})
# ARE committed.
.ds-sync/
ds-bundle/
/_screenshots/
design-system/node_modules/
design-system/dist/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
presence-config.json
